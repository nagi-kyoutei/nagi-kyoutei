# 🦀 ナギちゃん競艇予想 — 引き継ぎメモ（2026-07-12 全面更新）

別環境のClaude（カニ）へ。このファイルで全経緯・現在の仕様・運用・次やることが分かるで。
ユーザーは「田中」。田中と呼び、自分（Claude）は「カニ」と呼ばれる。**タメ口の関西弁**でやりとり。アプリUI文言も関西弁固定。

このファイル自体は `github.com/nagi-kyoutei/nagi-kyoutei` にも置いてある。会社PC・Mac・どの環境からでも、このリポジトリを見れば続きから動ける設計にしてある。

**⚠️2026-07-12〜 複数マシン運用に移行**: 田中は今後、会社PC（Windows）とMac（個人）の両方からナギちゃんの更新確認・予想作成を行う。どちらの環境のカニも、このリポジトリ（特にこのファイルと`data/`フォルダ）を正として動くこと。一方の環境で得た知見・修正は、必ずこのファイルまたはリポジトリに反映してから作業を終えること（会社PC⇄Mac間でファイルは自動同期されないため、GitHubリポジトリが唯一の共有場所）。過去の集計データ（6/27〜7/11分）は会社PCのローカルOneDriveフォルダにのみ存在していたが、2026-07-12にリポジトリの`data/`フォルダへアップロードしてどちらの環境からもアクセスできるようにした。

---

## 🌐 まず一番大事なこと：アプリは今、GitHub Pagesで公開済み
- **公開URL（アプリ本体）**: `https://nagi-kyoutei.github.io/nagi-kyoutei/`
- **リポジトリ（コード）**: `https://github.com/nagi-kyoutei/nagi-kyoutei`（Public）
- ローカルの `デスクトップ\クロコちゃん\index.html` は**もう正式版ではない**。正式版はGitHub Pages上のもの。今後の修正は**GitHubリポジトリ側のindex.htmlを編集**するのが正しい（ブラウザで直接編集できる。ローカルの開発サーバーserve.ps1はもう使わなくてよい）。
- **⚠️Public公開の注意（田中と合意済み・要アラート）**: リポジトリがPublicなので、scoreBoatsの重みや潮補正ロジックなど中身は誰でも閲覧・コピー可能な状態。今（予想屋で実績づくり段階）はこれでOKという合意。**「予想/舟券を販売する」話が具体化したら、カニから田中に必ずアラートを出すこと**（対策候補: GitHub Pro月$4でPrivate化、またはNetlify/Vercel等への移行）。

## 🤖 自動集計（GitHub Actions）が稼働中
毎日 **日本時間23:00** に、GitHub側のクラウドが自動で以下を実行する（田中は何もしなくてよい）：
1. `.github/workflows/collect.yml` がcronで起動（`workflow_dispatch`で手動実行も可）
2. `scripts/collect.js`（index.htmlのscoreBoats/makePicks/fetchTide等をNode.js用に移植したもの）が海水場の確定レースを予想・結果照合
3. `data/nagi_zenba_<日付>.json` として保存
4. `stefanzweifel/git-auto-commit-action` でリポジトリに自動コミット

2026-07-12に構築・手動実行テストで動作確認済み（唐津1R的中・回収258%を正しく検出・保存できた）。**自動発火（cronでの実行）は2026-07-12夜が初回**、まだ本番での成功は未確認の可能性がある→GitHubの「Actions」タブで実行履歴を確認すること。

### 今後データを見るとき
- **2026-07-12以降のデータ**: リポジトリの `data/nagi_zenba_<日付>.json` を見る（自動生成）
- **2026-06-27〜07-11のデータ**: 田中のローカルOneDriveフォルダ（`デスクトップ\クロコちゃん\nagi_zenba_*.json`）にのみ存在。GitHubには上げていない。分析時は両方参照する必要がある場合がある。

---

## プロジェクト概要と現在地
田中の競艇予想アプリ「ナギちゃん競艇予想」🦀。単一HTML PWA。
- **コンセプト＝海水場特化「潮の予想屋ナギちゃん」**（凪＝穏やかな海。瀬戸内しまなみは世界的な凪の聖地）。当初の宮島特化から2026-06-26に海水場17場へ拡張。
- 事業ロードマップ: ①田中利用→②改良→**③予想屋（買い目配信）で実績づくり ← 今ここ**→④販売。
- 販売形態の整理済み: **予想屋（買い目を売る）ならリアルタイム直前情報は不要**（田中が展示を見て予想を作ればよい）。アプリ自体を売る段階で中継サーバー（Cloudflare Workers等）＋規約確認が必要。
- 販売時の法務（先の話）: 特商法表記・断定表現NG（景表法）・課金の仕組み。

## 採点モデル（現行 = v2、2026-07-07実装）
`scoreBoats`の重み（`MODEL_VER='v2'`）:
```
courseScore*0.172 + clsScore*0.141 + localScore*0.125 + motorScore*0.125
+ exScore*0.219 + stScore*0.157 + sontaku + weather + tideAdj
```
- **8日858Rの216パターングリッドサーチで最適化**。旧v1（コース24%重視）=回収77% → v2（展示28%/ST20%相当、正規化済み）=回収109%（潮抜きバックテスト）。
- **核心＝展示タイムとSTを重視、コースを軽減**。「的中率はどの重みでも20-22%でほぼ同じ、回収率だけが変わる＝どの艇を上位に置くか（配当の高さ）で儲けが決まる」。
- **忖度補正（田中理論）は `SONTAKU_ON=false` で停止中**（宮島1Rで同期ダブルボス+12が裏目→1-2-3取りこぼしで田中が判断）。コードは残存、1行で復活可。
- **却下済みの調整（バックテストで実証済み、蒸し返さない）**: ①3着を厚くする買い方（点数増→的中率は上がるが回収率は下がる。6点ボックスが最良） ②カド(4コース)評価UP（回収88-90%に悪化。仮説①は誤りだった） ③当地・モーター重み増（悪化）。
- 買い目は**上位3艇ボックス6点固定**（全判定共通）。判定バッジ（堅い/中穴/荒れ）は表示のみ。

## 潮補正（17海水場・全場ON維持）
- `TIDE_HARBOR`に17場登録済み（tide736.net、pc/hc地点コード）。大村(24)のみ未登録（大村湾の観測点が見つからず保留）。淡水6場（桐生/戸田/多摩川/三国/びわこ/住之江）は対象外。
- 満ち(slope>=15)=イン加点／引き(slope<=-15)=センター加点、tideAdj最大±8。
- **8日通算バックテストで「潮ONがほぼ全場で優位」と確定**（宮島ON95% vs OFF34%=潮が命綱）。4-5日時点では「場によりOFFが良い」に見えたがサンプル不足の誤りだった→**全場ON維持が現方針**。1日単位では裏目の日もある（7/07は潮が若松6R7550円を逃した）が、通算判断を崩さない。
- 潮差ランキング: 宮島234/丸亀232/児島217/徳山197（瀬戸内4強）＞蒲郡148/津146/下関137…浜名湖78cm。

## アプリの主要機能（実装・検証済み）
- 3ステップUI（①本日取得→②場R選択→③予想）。予想のたび直前情報・潮を再取得。
- **展示クイック入力**（`showExInput`/`applyExInput`）: APIに直前情報が無い時、公式サイトを見て展示/ST/進入を手入力→🟢本物予想(hasPreview=true,manualEx=true)にできる。
- 成績ログ: localStorage(`kuroko_logs`)、場フィルタ、🟢本物予想のみ集計、結果自動照合(`checkResults`)、JSONバックアップ書出/読込。
- **記録に `model:'v2'` を保存**（新旧モデルの実戦比較用）。
- **未バックアップ警告**: 記録変更で`kuroko_dirty`フラグ→成績ログに💾警告カード、バックアップ保存で消える。
- **2026-07-12対応: boatraceopenapiのprograms/previews APIが `{programs:[...]}` から `{today:{programs:[...]},yesterday:{...}}` 形式に無告知で変更された**。`unwrapToday(json,key)` ヘルパーで新旧どちらでも動くように修正済み。外部APIは今後も無告知で変わりうるので、様子がおかしい時はまずAPIレスポンス構造を疑うこと。
- 検証時の注意: 単一HTMLなので`document.body.innerHTML`での文字列チェックはscriptソースにも一致する。UI検証は対象要素(logArea等)で。

## データ運用の変遷
- **2026-06-22〜07-11: A案（カニ手動集計）**。開催日の夜（23時前後）に田中が「カニ集計して」→カニがブラウザ操作で全海水場を一括集計、フルログをローカルOneDriveフォルダに保存。的中明細を必ず全列挙するのが田中の必須要望。弱点分析として🌊潮の外振り失敗／🎯3着取りこぼしを算出。
- **2026-07-12〜: 完全自動化**。上記「自動集計（GitHub Actions）」に移行。田中の手動依頼は不要になった。ただし過去データとの接続（両方見る必要がある分析）は当面残る。
- **保存の技術メモ（過去データ分析で必要になる可能性）**: ブラウザ内evalの戻り値がトークン超過するとtool-resultファイルに退避される→`perl -MJSON::PP -0777 -e 'binmode STDOUT,":utf8"; local $/; $s=<>; print JSON::PP::decode_json($s)' <退避ファイル> > <保存先>` でアンエスケープ保存（jq無し環境用）。ブラウザタブ内のwindow変数は同一タブなら持続するが、eval呼び出しごとにリセットされるサンドボックスもあるので過信しない。
- **日付マタギ注意（ローカルsonoya当時の教訓）**: programs/previewsのtoday.jsonは翌日切替（previewsが先に切り替わる、programsは`yesterday`キーに前日分が一時的に残ることがある＝救済の余地あり）。結果API(results/v3/日付)は過去も取れる。

## 蓄積データ
- **ローカルのみ（`デスクトップ\クロコちゃん\`）**: `nagi_zenba_2026-06-27〜07-11.json`（欠損は7/02・7/04のみ）。構造={date,kind,model?,count,logs:[{sid,stadium,rno,hasPreview,model,top3,combos,tide,result,hit,payoff,snapshot:[{no,course,pct,exTime,st,motor2,local2,cls}]}]}。6/28のみ展示なし参考予想版。7/11は前日API救済(参考予想版)。7/07からmodel:"v2"付き。
- `nagi_logs_2026-06-26.json`: 田中がアプリで手動記録した分（宮島24戦、6/26に自走達成・回収116%）。
- **GitHubリポジトリ（`data/`フォルダ）**: 2026-07-12以降の自動集計データ。
- バックテストはこれらのsnapshotから再計算で何でも試せる（結果配当はresults APIから取得）。

## 場の知見（8日通算、確定に近いもの）
- **稼ぎ頭（複数日で安定）**: 蒲郡・若松・福岡。宮島は潮ONで95%（潮が命綱）。
- **鬼門**: 徳山（本命1着62%・イン頭68%と"芯"は全場トップ級なのに、2-3着が荒れすぎて3連単回収17-28%＝3着問題の極端版）、唐津・児島も低迷。
- **津は「当たるが儲からない」**（的中率33%高いが配当安く回収81%）。
- 1日だけの高回収（下関401%等）は万舟一発の偶然、信用しない。
- 全体の弱点は「3着取りこぼし」だが、点数を増やす対策は回収率を下げるので**受け入れる**（対策は場の選別と精度向上）。

## 現在の最重要ミッション: v2の実戦検証
- v2実戦データ: 7/07回収44%（不調日）→7/08回収89%→7/09回収70%。日次でブレるが通算で見る。
- **数週間の通算で回収率100%超が確認できたら予想屋デビューの実績になる**。
- 宮島は7/16頃まで開催なし（〜13日間休み、7/02時点起点）。
- 回収率UPの残りの道: (1)v2の実戦実証 (2)稼ぐ場に絞る（場ランキング確定後）。

## 既知の課題・将来タスク
1. ~~GitHub Pages化~~ → **完了（2026-07-12）**。
2. 大村の潮位地点の追加調査。
3. 直前情報のリアルタイム化（販売フェーズ2、中継サーバー＋規約確認）。
4. 過去日の出走表は取得不可（today.jsonのみ。ただし`yesterday`キーに前日分が一時的に残ることがある）。結果だけは過去も取れる。
5. ローカルの過去データ（6/27〜7/11）をGitHubリポジトリにも上げるか検討（複数環境からの分析継続性のため）。
6. 自動集計は現状1日1回23時のみ。取りこぼし（深夜開催など）があれば実行タイミングの調整を検討。

## 田中の運用スタイル・好み
- 分析は「部分でなく通算」で見る癖がついている（田中自身が提案するようになった）。1日・1レースで重みを変えない（過学習防止）が合言葉。
- 的中率と回収率の違いは説明済み（儲けは回収率、100%超でプラス）。
- トークン節約意識あり。
- 田中はスクショで公式の直前情報を貼ってくれることがある（カニが読み取って本物予想を計算する連携も何度も実施）。
- GitHubの操作は「Create new file」でファイル名欄にフルパス（`.github/workflows/collect.yml`等）を直接入力する方式が確実（フォルダのドラッグ&ドロップは階層が崩れる事故が起きたので避ける）。
